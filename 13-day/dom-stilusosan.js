var numberOfClicks = 0;

function changeButton() {
    numberOfClicks++;

    //IDE dolgozz!
    var fancy - button = document.querySelector('.fancy-button'),
        classlist.toggle('change');
    if (numberOfClicks % 2) {
        numberOfClicks - style.background = 'red';
    } else
        numberOfClicks - style.background = '';
}