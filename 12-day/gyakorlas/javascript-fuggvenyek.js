// Innen jössz te:

function getWordsOfPacman() {
    return '';
}

function pacmanSays() {
    var pacmansaying = document.querySelector('pacmansaying');
    pacmansaying = getWordsOfPacman() {
        if (getWordsOfPacman === true); {
            else
                return false;
        }
    }
}
pacmansaying.innerHTML = true;

// Ez pedig a játékhoz kell ;-)
var gameParent = document.querySelector('.game-container');